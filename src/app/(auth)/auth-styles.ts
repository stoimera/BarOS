/** Shared auth pages (login/register) — matches Urban Lounge amber theme; overrides default blue focus ring. */
export const AUTH_INPUT_CLASSNAME =
  "border-amber-300 dark:border-amber-700 " +
  "focus-visible:border-amber-500 focus-visible:ring-amber-500/50 focus-visible:ring-[3px] " +
  "dark:focus-visible:border-amber-400 dark:focus-visible:ring-amber-400/50"
